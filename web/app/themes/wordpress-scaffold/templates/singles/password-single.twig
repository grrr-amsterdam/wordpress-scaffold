<form class="password-form" action="{{ site.site_url }}/wp-login.php?action=postpass" method="post">
    <label for="pwbox-{{ post.ID }}">{{ __('Password', 'grrr') }}:</label>
    <input class="password-box" name="post_password" id="pwbox-{{ post.ID }}" type="password" placeholder="Password" size="30" maxlength="255" />
    <button type="submit" name="submit">{{ __('Submit', 'grrr') }}</button>
</form>

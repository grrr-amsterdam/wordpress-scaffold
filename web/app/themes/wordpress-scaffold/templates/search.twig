<section class="search">

    {% include 'partials/page-header.twig' with {
        title: post.title,
    } only %}

    {% for post in posts %}
        {% include [
            'previews/preview-' ~ post.post_type ~ '.twig',
            'preview.twig',
        ] %}
    {% endfor %}

    {% include 'partials/pagination.twig' with {
        pagination: posts.pagination({
            show_all: false,
            mid_size: 3,
            end_size: 2,
        }),
    } only %}

</section>

<article class="author">
    {% include 'partials/page-header.twig' with {
        title: post.title,
    } only %}

    {% for post in posts %}
        {% include [
            'previews/' ~ post.post_type ~ '-preview.twig',
            'preview.twig',
        ] %}
    {% endfor %}
</article>

<ul>
    {% for item in menu.items %}
    <li>
        <a href="{{ item.link }}" aria-current="{{ item.current ? 'page' : '' }}">
            <span>{{ item.title }}<span>
        </a>
    </li>
    {% endfor %}
</ul>
